/**
 * Configurable brand label. Swap these values (or feed them from a CMS/DB in a
 * later phase) — no component should hardcode this copy.
 */
export const branding = {
  wordmark: 'Living Luxury Lab',
  /** Short label used in tight layouts. */
  shortMark: 'LLL',
  tagline: 'Art placement studio',
};
